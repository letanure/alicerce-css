/*

# Typography

The default font is _Noto Sans_

*/

@import url('https://fonts.googleapis.com/css?family=Noto+Sans:400,700')

body
  font-family var(--fontFamily)
  font-size var(--fontSize)
  line-height calc(var(--verticalRhythm) * 2)
